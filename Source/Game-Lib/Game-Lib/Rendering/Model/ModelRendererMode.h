#pragma once

namespace ModelRendering
{
    // Selects which model renderer owns newly loaded map placements during the migration.
    bool UseMeshletModelRenderer();
    bool ShowModelCullReasons();
}
